#!/usr/bin/env bash

. scripts/env.sh

mkdir -p $TOP/out/build/drm
cd $TOP/out/build/drm
$TOP/drm/autogen.sh --host=${CROSS_COMPILE%"-"} --prefix=$PREFIX --enable-tegra-experimental-api
make -j4 && make install
