#!/usr/bin/env bash

. scripts/env.sh

mkdir -p $TOP/out/wayland
cd $TOP/out/wayland
$TOP/wayland/autogen.sh --host=${CROSS_COMPILE%"-"} --prefix=$PREFIX --disable-documentation
make -j4 && make install
