#!/usr/bin/env bash

. scripts/env.sh

mkdir -p $TOP/out/build/weston
cd $TOP/out/build/weston
mkdir -p m4
$TOP/weston/autogen.sh --host=${CROSS_COMPILE%"-"} --prefix=$PREFIX
make -j4 && make install
